<!DOCTYPE html>
<html>
    <head>
      <title>Mastering Mom</title>
      <%= render 'layouts/stylesheets' %>
      <%= javascript_include_tag :defaults %>
      <%= csrf_meta_tag %>
    </head>

    <body>

    <div class="container">
      <header>
        <div class="mm_titles round">
          <h1 class="mm_main">Mastering "Mom"</h1>
          <h3 class="mm_sub">... one sticky, stinky, mini disaster at a time.</h3>
        </div>
      </header>

      <%= yield %>
    </div>

    </body>
</html>
